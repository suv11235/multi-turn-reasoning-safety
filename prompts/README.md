# Prompt Assets

- `prompts_refusal_benchmark.jsonl` – master prompt list with schema-compliant entries.
- `prompt_schema.md` – explanation of required fields and how to extend the set.

Use these prompts for zero-shot evaluation with DeepSeek-Distill-Llama-3.1-8B as described in `datasets/inference_config.yaml`.
